import { describe, it, expect } from 'vitest';
import { Client } from '../src/index.js';

describe('{{ name }} client', () => {
  it('instantiates without throwing', () => {
    expect(
      () => new Client({ baseUrl: 'https://example.com' })
    ).not.toThrow();
  });
});
